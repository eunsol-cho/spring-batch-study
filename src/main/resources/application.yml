spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_batch?serverTimezone=Asia/Seoul
    username: root
    password: qwer12322!

  batch:
    jdbc:
      initialize-schema: always
    job:
      # names : job4
      # 스프링 배치에서 잡이 자동으로 실행되지 않게 설정
      enabled: true
